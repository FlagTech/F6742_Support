# Chapter 1

## 1-2 安裝 OpenCode

Windows Terminal 下載: <https://aka.ms/terminal>

Scoop 官方網站: <https://scoop.sh/>

Windows（透過 Scoop 安裝）：

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
Invoke-RestMethod -Uri https://get.scoop.sh | Invoke-Expression
scoop install opencode
```

Windows 更新 OpenCode：

```powershell
scoop update opencode
```

```powershell
cd $HOME
mkdir test; cd test
opencode
```

macOS（Kitty 終端機）：

Kitty 下載: <https://sw.kovidgoyal.net/kitty/binary/>

```sh
curl -L https://sw.kovidgoyal.net/kitty/installer.sh | sh /dev/stdin
```

Linux 手動設定 Kitty（選用）：

```sh
mkdir -p ~/.local/bin ~/.local/share/applications
ln -sf ~/.local/kitty.app/bin/kitty ~/.local/bin/kitty
ln -sf ~/.local/kitty.app/bin/kitten ~/.local/bin/kitten
cp ~/.local/kitty.app/share/applications/kitty.desktop ~/.local/share/applications/
sed -i "s|Icon=kitty|Icon=$HOME/.local/kitty.app/share/icons/hicolor/256x256/apps/kitty.png|g" ~/.local/share/applications/kitty.desktop
sed -i "s|Exec=kitty|Exec=$HOME/.local/kitty.app/bin/kitty|g" ~/.local/share/applications/kitty.desktop
```

macOS / Linux（官方安裝腳本）：

OpenCode 官方網站: <https://opencode.ai/>

```sh
curl -fsSL https://opencode.ai/install | bash
```

macOS / Linux 更新 OpenCode：

```sh
opencode upgrade
```

```sh
cd $HOME
mkdir test && cd test
opencode
```

離開 OpenCode：

```text
/exit
```

## 1-3 第一個任務

```text
我要使用軟體套件管理程式，在 macOS/Linux 上，幫我確認 Homebrew 是否可用，沒有的話就安裝，僅限當前使用者；在 Windows 上，幫我確認 Scoop 是否可用，沒有的話就安裝，僅限當前使用者。
```

```text
確認我有沒有安裝 scoop 或是 homebrew
```

```text
幫我安裝 Git
```

```text
我也要安裝 uv
```

```text
安裝 nvm 然後用 nvm 安裝最新版本的 Node.js
```

離開 OpenCode、重新開啟終端機後手動驗證：

```powershell
node --version
uv --version
nvm --version
git --version
```

```text
幫我裝 vs code，不要在那邊先告知我方式
```

接續交談階段：

```text
/sessions
```

```text
安裝 vscode
```

```text
opencode -c
opencode -s <session-id>
```

合併 opencode.json 設定的提示詞範本：

```text
你好，這是我目前的 opencode.json
[ 完整的 json 設定 ]

我想加入以下設定：

[ 要增加的 json 項目 ]

請將新設定整合到原有內容中，並輸出完整且符合 JSON 語法的設定。請不要省略原本的設定，也不要使用註解。
```

實際範例（新增 chrome-devtools MCP）：

```text
你好，這是我目前的 opencode.json
{
  "$schema": "https://opencode.ai/config.json",
  "compaction": {
    "auto": true,
    "prune": true,
    "reserved": 10000
  },
  "permission": {
    "bash": {
      "rm*": "ask",
      "Remove-Item*": "ask"
    }
  }
}
我想加入以下設定：
"mcp": {
  "chrome-devtools": {
    "type": "local",
    "command": [
      "npx",
      "-y",
      "chrome-devtools-mcp@latest",
      "--autoConnect"
    ],
    "enabled": false,
    "timeout": 20000
  }
}
請將新設定整合到原有內容中，並輸出完整且符合 JSON 語法的設定。請不要省略原本的設定，也不要使用註解。
```
