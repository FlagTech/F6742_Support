# Chapter 9

## 9-1 設計應用程式功能與架構

```text
請先上網調查 OpenCode 最新的 AGENTS.md 使用方式與相關慣例，再幫我產生適合 OpenCode 使用的 AGENTS.md，以及內容只有 @AGENTS.md 的 CLAUDE.md。
規則需要求 OpenCode 在專案開始時先詢問我需求，並自行建立 PRD.md、.harness/progress.yaml 與 .harness/handoff.yaml；之後依 PRD 執行工作、追蹤進度並留下跨 Session 的交接資訊。
PRD.md 是需求的唯一來源，Agent 不可自行猜測或修改需求。
```

```text
我希望開發一個能夠在桌面顯示浮動按鈕，點擊後在按鈕下方展開資訊欄，跨平台的 Python 桌面應用程式。（雖然是跨平台，但只需要確保架構是跨平台，測試和使用只在當前平台）

主要功能如下：
1. 浮動入口與資訊欄
- 桌面上顯示可自由移動的浮動按鈕
- 點擊浮動按鈕後，在按鈕下方展開資訊欄
- 再次點擊浮動按鈕或按下關閉按鈕即可收合
- 資訊欄可保持在其他一般視窗上方
- 各功能以卡片形式呈現，並允許收折與拖曳排序

2. 時間資訊
- 顯示目前時間與日期
- 提供 12 小時制與 24 小時制切換
- 時間資訊會自動更新，不需要手動重新整理

3. 常用網址捷徑
- 顯示使用者設定的常用網站捷徑
- 允許新增、刪除與排序網址捷徑
- 點擊捷徑後，使用系統預設瀏覽器開啟網站
- 無效或無法開啟的網址不影響其他功能運作

4. RSS、系統狀態、本地筆記與待辦清單
- 自動讀取 RSS 並顯示最新文章
- 顯示 CPU、記憶體與磁碟使用量
- 允許新增、編輯、搜尋與刪除本地筆記
- 允許建立、完成、刪除與排序待辦項目

請以 Plan Agent 的角度，先使用 Question 工具詳細詢問我，例如技術實作、UI/UX 設計，不要問顯而易見的問題，要挖掘我可能沒想到的難點。訪談完成後，請設計完整的規格文件。現在先不要建立程式碼。
```

```text
根據我們剛才確定的需求與架構，在專案根目錄建立 PRD.md。

PRD.md 至少需要包含：
1. 專案背景與目標
2. 目標使用者
3. 主要目標功能範圍
4. 額外目標與延伸功能
5. 使用者操作流程
6. 功能需求
7. 非功能需求
8. 技術架構
9. 資料保存方式
10. 安全與權限邊界
11. 錯誤與降級行為
12. 驗收條件
13. E2E 測試範圍
14. 開發里程碑

現在仍然不要建立實作程式碼。
```

```text
現在請以即將接手本專案的 Build Agent 角度，檢查目前的：
- PRD.md
- AGENTS.md
- CLAUDE.md
- .harness/progress.yaml
- .harness/handoff.yaml

找出任何會妨礙實作的模糊、矛盾或缺漏。

請特別檢查：
- 技術架構是否已確定
- 主要實作範圍是否明確
- 每個核心功能是否有驗收條件
- Agent 是否知道應先讀取哪些檔案
- 進度與交接格式是否可持續更新
- E2E 測試是否有明確範圍

若沒有重大問題，再更新 progress.yaml 和 handoff.yaml，標記規劃階段完成。
```

```text
現在請建立專案初始 commit。
```

## 9-2 執行實作和除錯

```text
請依照規範，接手本專案，並實作一個 Phase。
```

當 Agent 鬼打牆時：

```text
停止繼續提出新方案，也先不要修改程式。

請列出：
1. 目前已知事實。
2. 最多 3 個可能假設。
3. 每個假設對應的最小驗證實驗。

選擇成本最低的實驗立即執行。
在取得新證據前，不得提出新的修補方案。
```
如果問題持續，新開一個對話，然後

```text
[ 目前的問題 ]

請列出：
1. 目前已知事實。
2. 最多 3 個可能假設。
3. 每個假設對應的最小驗證實驗。

選擇成本最低的實驗立即執行。
在取得新證據前，不得提出新的修補方案。
```

debugging-methodology Skill GitHub 文件: <https://github.com/DevelopersGlobal/ai-agent-skills/blob/main/skills/debugging-methodology/SKILL.md>

```text
/debugging-methodology 剛才測試時發現按鈕跟浮動窗口沒辦法置頂，請協助修正。
```

```text
請更新目前的進度，再將目前的實作 commit
```

## 9-3 完善專案各項功能

```text
我希望在筆記功能中新增 AI 摘要功能，請協助我釐清需求與設計規格，並確保以下事項：
1. 使用者可以選取筆記內容，並將其交給 AI 產生摘要。
2. AI 產生的摘要會顯示在對應的筆記卡片中。使用者可以選擇是否替換原始筆記內容，或取消來保留原始內容。也能選擇複製來將摘要複製到剪貼簿。
3. 若未設定 API Key 或 AI 服務無法使用，本地筆記功能仍可正常運作。
4. API Key 僅儲存在本機安全設定中，不寫入版本控制。
請列出可能的技術實作方案、UI/UX 設計考量、資料保存方式，以及錯誤處理機制，並使用 Question 工具協助我釐清後，提出建議的設計規格。
```

```text
OK，請將設計規格更新到 PRD,progress.yaml 和 handoff.yaml，並標記目前 Phase 的任務完成後，再將這些更新 commit。
```

```text
請接手目前 Phase 的任務，並依照設計規格實作 AI 摘要功能。
```

DeepSeek API 端點: <https://api.deepseek.com/>

```text
我想要實作 AI 待辦清單生成：能將一件事情拆解成多個子任務，並生成待辦清單。如果覺得 AI 生成的子任務不符合需求，使用者可以在重試時提供更多細節，讓 AI 生成更符合需求的子任務。
請使用 Question 工具協助我釐清需求與設計規格，並確保以下事項：
1. 使用者可以輸入一件事情，並將其交給 AI 生成子任務。
2. AI 生成的子任務會顯示在待辦清單中
3. 使用者可以選擇是否接受 AI 生成的子任務，或重新輸入更多細節來讓 AI 生成更符合需求的子任務。
4. 若未設定 API Key 或 AI 服務無法使用，本地待辦清單功能仍可正常運作。
5. API 端點、模型和 API Key 使用和 AI 摘要功能相同。
```
