# Chapter 3

## 3-1 OpenCode 操作介紹

斜線指令：

```text
/help
/models
/sessions
/undo
/new
/redo
/compact
/exit
```

接續或查看統計：

```text
opencode -c
opencode -s <session-id>
opencode stats
```

範例提示詞：

```text
請檢查設定檔有沒有問題。
```

```text
請檢查 @opencode.json 有沒有互相衝突的權限規則。
```

提示：也可以用 `@` 加上檔名引用檔案（例如 `@AGENTS.md`）。

## 3-2 從 CC 也能快速上手：Claude Code 專案

ClaudeProjectDemo GitHub 專案: <https://github.com/FlagTech/ClaudeProjectDemo>

```sh
git clone https://github.com/FlagTech/ClaudeProjectDemo.git
cd ClaudeProjectDemo
opencode
```

```text
請先閱讀目前的專案，不要修改任何檔案。

請整理以下內容：
1. 這個專案的用途
2. 目前使用的技術
3. 已經具備哪些功能
4. 主要檔案分別負責什麼
5. 專案中記錄了哪些開發規則
6. 如果要繼續開發，應該先閱讀哪些文件
```

```text
請閱讀 CLAUDE.md，將其中適用於所有 Coding Agent 的專案說明與開發規則整理到專案根目錄的 AGENTS.md。

請注意：
1. 不要修改任何程式碼。
2. 不要改變原有規則的意思。
3. AGENTS.md 應保留專案說明、開發規則、專案結構與執行方式。
4. 完成後請說明 CLAUDE.md 與 AGENTS.md 各自應負責什麼。
```

```text
請整理目前讀取到的專案規則，並說明規則分別來自哪些檔案。
```

```text
要為 Focus Board 加入以下功能：
- 全部、未完成、已完成三種篩選模式
- 顯示目前選取的篩選狀態
- 清除所有已完成項目的按鈕
- 沒有已完成項目時停用清除按鈕

請先幫我規劃如何實作，並列出需要修改的檔案與主要邏輯。另外最後也應更新相對應的文件。
```

```text
請依照剛才的規劃實作這些功能。

完成後請：
1. 檢查新增、刪除、切換完成狀態與 localStorage 是否仍正常。
2. 說明修改了哪些檔案。
3. 列出手動測試方式。
4. 靜態測試即可。
```

```text
請重新檢查這次修改是否符合新需求，並確認沒有破壞 docs/PRD.md 中原有的功能與驗收條件。
```

## 3-3 如何建立跨 Agent 的專案架構

```text
請先接手目前的專案，不要立即修改檔案。

請依序：
1. 閱讀 AGENTS.md。
2. 閱讀 README.md 與相關需求文件。
3. 閱讀 progress.yaml 與 handoff.yaml。
4. 執行 git status，檢查目前工作區。
5. 查看與目前工作有關的 Git 差異。
6. 整理已完成、未完成、已知問題與下一步。
7. 確認文件內容是否與實際程式碼一致。

如果發現資訊矛盾或有無法確認的修改，請先提出，不要自行覆蓋。
```
