# Chapter 5

## 5-1 什麼是 MCP 與 Skills？

````markdown
---
name: summarize-changes
description: 當有未提交的程式碼變更時，摘要說明並回報潛在風險。
---

## 目前的程式碼變更
請執行以下命令取得尚未提交的變更：
```bash
git diff HEAD
```
## 說明指令
請以兩三個要點摘要取得變更的內容，並回報可能造成的潛在風險。

````

## 5-2 Browser MCP

開啟 Chrome 並連線啟用遠端偵錯：

Chrome 遠端偵錯頁面: `chrome://inspect/#remote-debugging`

用 VS Code 開啟 opencode.json：

```sh
cd ~ | code .config/opencode/opencode.json
```

在 opencode.json 中加入 chrome-devtools MCP 設定：

```json
{
  "mcp": {
    "chrome-devtools": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "chrome-devtools-mcp@latest",
        "--autoConnect"
      ],
      "enabled": true
    }
  }
}
```

下載範例專案並啟動伺服器：

test_buyhouse 更新資料分支: <https://github.com/FlagTech/test_buyhouse/tree/update_data>

```sh
git clone -b update_data https://github.com/FlagTech/test_buyhouse.git
cd test_buyhouse
uv run wsgi.py
```

臺灣房價地圖本機網站: <http://127.0.0.1:5000>

```text
請幫我開啟 http://127.0.0.1:5000，讀取瀏覽器 Console 的錯誤訊息，找出圖表無法顯示的原因
```

```text
好啊，直接修正，並且重新載入網頁確定修正圖表問題
```

```text
我在桌面上有一份 sales_data.csv，請幫我在 Google 表單中新增此份資料，把 CSV 中的資料整理後填入。
```

```text
直接打開 google 文件新增一個試算表
```

## 5-3 Playwright MCP

Microsoft Playwright MCP GitHub 專案: <https://github.com/microsoft/playwright-mcp>

DeepWiki MCP 文件: <https://docs.devin.ai/work-with-devin/deepwiki-mcp>

```text
請幫我查詢 OpenCode Docs 的 MCP 設定格式，並且幫我把以下格式轉換成可以貼上到 OpenCode 設定檔的 JSON 格式：
{
  "mcpServers": {
    "deepwiki": {
      "url": "https://mcp.deepwiki.com/mcp"
    }
  }
}
```

在 opencode.json 中加入 deepwiki 與 playwright MCP 設定：

```json
{
  "mcp": {
    "deepwiki": {
      "type": "remote",
      "url": "https://mcp.deepwiki.com/mcp",
      "enabled": false
    },
    "playwright": {
      "type": "local",
      "command": ["npx", "@playwright/mcp@latest"],
      "enabled": false
    }
  }
}
```

還原程式碼並重啟伺服器：

```sh
git restore .
uv run wsgi.py
```

```text
請幫我查詢 DeepWiki MCP，了解 FlagTech/test_buyhouse 專案的架構、主要功能與程式碼邏輯，再查詢有關 microsoft/playwright-mcp 的功能，並整理成一份 Playwright MCP 自動化測試清單，存到 test_plan.md 內
```

```text
請依照 test_plan.md 的測試清單，透過 playwright mcp 開啟 http://127.0.0.1:5000/，並測試我的網頁應用程式功能是否正常
```

```text
請幫我修正所發現的錯誤
```

```text
請透過 playwright-mcp 開啟 http://127.0.0.1:5000/ 測試 Bug 是否真的修正
```

```text
先修復 plugin 的問題，然後幫我依序點擊 SVG 地圖中「全部」的縣市，確認每個縣市的資料都正常
```

## 5-4 使用 Skills 和 Playwright CLI

```sh
npm install -g @playwright/cli@latest
playwright-cli install-browser
playwright-cli install --skills
playwright-cli --help
```

斜線指令：

```text
/playwright-cli
```

```text
請開啟臺灣房價地圖首頁（http://127.0.0.1:5000/），對網站功能進行主要測試
```

```sh
playwright-cli open http://127.0.0.1:5000/
playwright-cli snapshot
playwright-cli console
playwright-cli click e20
playwright-cli close
```

```text
請將剛剛使用 playwright-cli 的完整測試流程建立成 Skill，依序測試臺灣房價地圖網站的核心功能。在專案的 .opencode/skills/playwright-test/ 建立一個 SKILL.md，frontmatter 設定：name 為 playwright-test，description 為摘要這個 Skill 的用途。
```

```sh
playwright-cli screenshot --filename=debug.png
```

```text
請在 playwright-test SKILL 中新增測試項目，依序點擊 SVG 地圖中的所有縣市，確認每次點擊後城市數值都會更新
```
