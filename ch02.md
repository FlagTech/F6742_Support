# Chapter 2

## 2.1 什麼是 AGENTS.md？

```text
/init 請幫我建立專案規範，它用的是 uv 環境，而版本對應的是 Python 3.13
```

```text
這個專案應該要用什麼 python 環境，根據你得到的訊息直接回答
```

```text
Python 專案在這邊應該使用什麼工具管理執行環境
```

用 VS Code 開啟全域 AGENTS.md：

```text
code "$HOME/.config/opencode/AGENTS.md"
```

## 2.2 與 Claude Code 專案的相容

```text
code test_claude/
```

```text
請幫我執行專案並告訴我執行結果
```

## 2.3 用 OpenCode 寫一個 PDF 浮水印工具

```text
設計一個可以幫 PDF 檔加上浮水印文字的 Python 程式：
- 使用 'Confidential' 作為浮水印文字
- 浮水印 45 度斜角半透明加在頁面中央，一頁只加上一個浮水印文字
- 同一份 PDF 檔可能有不同尺寸的頁面
- 加上浮水印的 PDF 檔以原 PDF 檔名加上 "_wm" 命名
- 設計好請自行生成一份 PDF 檔 test_mixed.pdf，包含直式 Letter、A4 和橫向 Letter、橫向 A4 進行測試，測試轉換通過後開啟 PDF 檔給我檢查
- 直接在此資料夾下設計
```

範例 PDF（個人資料保護法）: <http://bit.ly/4twbHPN>

在新終端機下載測試檔並執行：

```sh
curl -Ls -o law.pdf http://bit.ly/4twbHPN
uv run watermark.py law.pdf
open law_wm.pdf
```

## 2.4 切換權限模式

接續前次交談：

```text
opencode -c
```

```text
加入額外的命令列參數：
- text：設定浮水印文字，未指定時使用原預設值
```

```text
希望能讓中文字也能正常傳入以及被設定
```

```text
開始實作
```

```text
加入 `--output` 引數指定輸出檔名
```

復原與提交：

```text
/undo
```

```text
/init
```

```text
提交目前成果，PDF 檔都略過不要提交
```

```text
幫我設定 git global 的 user.name 設為 "xxx"，user.email 設為 "xxx@example.com"，然後再提交一次
```
