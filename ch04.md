# Chapter 4

## 4-1 Subagent 簡介

建立與查詢 Subagent：

```sh
opencode agent create --path .opencode/agents
opencode agent create
opencode agent list | awk '/^technical-translator/,/^$/'
```

Windows 執行 awk 需要 gawk：

```powershell
scoop install gawk
```

```text
將專案內 en.txt 內容翻譯成繁體中文，並另存成 zh-tw.txt
```

```text
幫我用 @technical-translator 將專案內的 en.txt 中的內容翻譯成繁體中文
```

## 4-2 多 Subagent 並行運作的威力

```text
請分別用 C、Python、Java、C++、Go、Rust、JavaScript 寫出 bubble、merge、quick、heap、insertion 五種排序法，每個程式碼都用該程式語言的檔案格式獨立儲存，只要寫出來存檔，不用編譯執行與測試，不使用 Subagent，並透過一開始確認時間和最終完成時的時間差，計算花了多少時間完成
```

建立專案級 Subagent（每個語言各執行一次 `opencode agent create --path .opencode`，共新增 7 次）：

```text
name: c-code-engineer 你是一個 C 語言工程師，你負責用 C 語言寫出要求的程式碼，並儲存於 .c 檔
```

```text
name: python-code-writer 你是一個 Python 工程師，你負責用 Python 寫出要求的程式碼，並儲存於 .py 檔
```

```text
name: java-code-generator 你是一個 Java 工程師，你負責用 Java 寫出要求的程式碼，並儲存於 .java 檔
```

```text
name: cpp-code-writer 你是一個 C++ 工程師，你負責用 C++ 寫出要求的程式碼，並儲存於 .cpp 檔
```

```text
name: go-code-engineer 你是一個 Go 語言工程師，你負責用 Go 寫出要求的程式碼，並儲存於 .go 檔
```

```text
name: rust-code-engineer 你是一個 Rust 工程師，你負責用 Rust 寫出要求的程式碼，並儲存於 .rs 檔
```

```text
name: javascript-code-engineer 你是一個 JavaScript 工程師，你負責用 JavaScript 寫出要求的程式碼，並儲存於 .js 檔
```

確認已建立的 Subagent：

```sh
ls .opencode/agents/*
```

```text
請分別用 c-code-engineer、python-code-writer、java-code-generator、cpp-code-writer、go-code-engineer、rust-code-engineer、javascript-code-engineer 寫出 bubble、merge、quick、heap、insertion 五種排序法，只要寫出來存檔，不用編譯執行與測試，並透過一開始確認時間和最終完成時的時間差，計算花了多少時間完成
```

## 4-3 用獨立上下文建立一個客觀的程式碼審查員

OpenCode 內建工具文件: <https://opencode.ai/docs/zh-tw/tools/#內建工具>

```text
請用 personal-code-reviewer 分別審查直接生成的排序法程式碼 (/Users/test_oc/coding_test/sorting_algorithms)，以及透過 Subagent 生成的排序法程式碼 (/Users/test_oc/coding_test_subagent/sorting)，兩者其中的 Rust 程式碼，比較兩者的程式碼品質
```

## 4-4 讓多個 Subagent 組成專業團隊

中央社新聞網站: <https://www.cna.com.tw>

```text
請搜尋今天國際的十大新聞，並參考 https://www.cna.com.tw 的版面風格，做成有十個新聞標題，其中有一個是頭條新聞，每則標題下附有一段摘要，且十則標題都可以點擊查看新聞內容的響應式網頁
```

```sh
ls .opencode/agents
```

```text
請先同時使用 international-news-aggregator 蒐集國際新聞、cna-style-researcher 研究網站風格，兩者完成後再使用 news-summarizer 根據蒐集到的新聞撰寫每則摘要，最後使用 news-page-builder 根據新聞、摘要與風格指南，完成網頁
```
