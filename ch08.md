# Chapter 8

## 8-1 從會使用 AI，到掌握工作系統

### 重點整理

- **Harness** 是圍繞 Agent 所建立的工作系統，核心不是「AI 產生 code 給你」，而是「你指揮 AI 艦隊」。
- 只靠提示詞無法長期維持工作品質，重點會從 **Prompt** 延伸到 **Context**，再擴大成 **Harness**：

| 層次 | 解決的問題 | 書中的做法 |
|---|---|---|
| Prompt | 這一次要做什麼？ | 描述需求、限制與交付內容（如第 1 章的安裝環境） |
| Context | Agent 需要知道什麼？ | AGENTS.md、專案檔案、對話紀錄與交接文件（如第 2、3 章） |
| Harness | 怎麼讓工作持續以相同標準進行？ | Agent 分工、Skills、MCP、Permission、測試、Git 與成本限制（如第 4~7 章） |

### 六層控制力

| 章節 | 你建立的控制力 | 留下來的工作資產 |
|---|---|---|
| 第 2 章 | 專案上下文控制 | AGENTS.md、/init、測試要求與 Git 紀錄 |
| 第 3 章 | 平台自由度控制 | CLAUDE.md／AGENTS.md、handoff 與具有彈性的專案規範 |
| 第 4 章 | 角色分工控制 | Plan、Build、Subagent 與各角色的權限、模型、上下文 |
| 第 5 章 | 工具與流程控制 | MCP、Skills、瀏覽器操作與可重複的回歸測試 |
| 第 6 章 | 安全與介入控制 | Permission、Command、Plugin 與事件通知 |
| 第 7 章 | 模型與成本控制 | 模型分工、預算上限、資料政策與 Provider 備援 |

### 提示詞範例

書中以浮水印工具為例，說明「一句話交代功能，但沒交代上下文」：

```text
幫我把 PDF 每一頁加上半透明浮水印，文字要能由命令列參數指定。
```

這句話沒告訴 Agent：使用哪個 Python 套件管理器、Python 版本、程式寫完後要跑哪些靜態測試。這些都屬於 Context 的範疇，可透過 AGENTS.md、Skill、Permission 與 Git 補齊。

## 8-2 從三個維度配置你的 Harness

### 分派任務時至少說明四件事

1. **輸入**：它可以讀哪些檔案、前一個角色交付了什麼。
2. **輸出**：要回傳程式碼、研究摘要、測試報告，還是修改建議。
3. **權限**：只能讀取，或可以修改檔案、執行命令、連接外部服務。
4. **驗收**：可以用測試、畫面、差異檔或其他證據判斷成果。

> 任務單純時，讓主 Agent 直接完成更省事、省成本；只有子任務彼此獨立且有足夠工作量時才拆分 Subagent。

### 新任務的判斷順序

1. 確認任務與驗證完成的條件。
2. 判斷是否需要拆分，以及哪些工作能並行。
3. 替每個角色配置上下文、工具與權限。
4. 依能力、資料風險與成本選擇模型和平台。
5. 根據驗證完成的條件決定是否完成，或需要再調整。

> 連完成條件都沒說清楚，就先討論要開幾個 Subagent 或改用旗艦模型，多半會浪費許多時間與成本。

## 8-3 當 Harness 走偏時：五種反模式

### 五種反模式簡表

| 反模式 | 看得見的症狀 | 付出的代價 | 修正方式 |
|---|---|---|---|
| 過度拆分 | 很小工作也使用多 Subagent | 啟動、讀取上下文與整合報告的成本超過並行收益 | 只拆分獨立且有足夠工作量的任務 |
| 迷信模型能力 | 小修改也用旗艦模型，失敗就繼續換更強模型 | 費用增加，模糊需求與錯誤上下文仍未解決 | 先修正規格、上下文與驗收，再依能力門檻升級 |
| 微管理 | 人類逐次核准低風險步驟，盯著每個工具的呼叫過程 | 人類成為流程瓶頸，Agent 無法連續完成工作 | 事前設定安全邊界，只在高風險、異常與完成節點介入 |
| 放棄理解 | AI 說完成就直接接受，沒有理解設計邏輯或測試範圍 | 問題進入正式環境後，團隊無法判斷與維修 | 保留人工審查，要求可重現的測試與證據 |
| 無底洞除錯 | 對同一錯誤反覆改寫 prompt，沒有取得新資訊 | Token 與時間持續消耗，對話上下文越來越亂 | 連續兩次同型修正失敗就停手，重新蒐集證據 |

### 反模式提示詞範例

書中以「迷信模型能力」為例：

```text
幫我把網站變好看
```

即使換更強的模型也無法替你決定專案需求。模型表現不佳時，先檢查：需求是否具體？相關檔案是否齊全？完成條件能否測量？

### 無底洞除錯的停損步驟

連續兩次修正失敗時，先停下來：

1. 閱讀錯誤發生位置與相關程式碼，不只看最後一行訊息。
2. 比較 Git diff，確認前兩次修改是否真的碰到根因。
3. 建立最小重現案例，縮小到能單獨測試的範圍。
4. 整理或重開 Context，再決定是否換模型、Provider 或平台。

> 第二次失敗不代表放棄，只要加入新證據就能形成下一個假設；沒有新證據的重試只會把除錯變成抽獎。

### 駕馭者檢核表

把任務交給 Agent 前，先確認：

- 誰負責規劃、執行、研究與最後整合？真的需要拆成多個角色嗎？
- Agent 會讀取哪些專案資料？穩定規則是否已留在 AGENTS.md 或相關檔案？
- 需要哪些工具？哪些操作可以自動執行，哪些必須先問人？
- 任務適合哪個模型與 Provider？資料能否送往該服務，預算上限是多少？
- 什麼證據才算完成？測試失敗或結果不符時，回復點在哪裡？
- 連續失敗幾次後是否要停下來重整問題，而不是繼續重投 Prompt？
